# CRA on Vercel sets CI=true, which promotes ESLint warnings to errors and
# fails the build on pre-existing lint issues unrelated to this PR. We don't
# want CI=true semantics here — Vercel preview/prod deploys should ship even
# when there are non-fatal lint warnings.
CI=false
